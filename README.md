# 小杰的个人作品集网站

基于 React + TypeScript + Vite + Tailwind CSS 开发的个人作品集网站。

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **React Router** - 路由管理
- **Framer Motion** - 动画效果

## 项目结构

```
src/
  components/        # 组件目录
    Header.tsx      # 头部导航
    Hero.tsx        # 首页 Hero 区域
    About.tsx       # 关于我
    Projects.tsx    # 项目展示
    Contact.tsx     # 联系方式
    Footer.tsx      # 底部
  data/             # 数据目录
    personal.ts     # 个人信息配置（头像、标题等）
    projects.ts     # 项目数据
    skills.ts       # 技能数据
  App.tsx           # 主应用组件
  main.tsx          # 入口文件
  index.css         # 全局样式
public/
  images/           # 图片资源目录（头像、项目截图等）
```

## 安装依赖

```bash
npm install
```

## 开发

```bash
npm run dev
```

## 构建

```bash
npm run build
```

## 预览构建结果

```bash
npm run preview
```

## 设计规范

- 深色主题（背景 #0a0a0a，文字 #ffffff）
- 使用渐变色作为强调色
- 平滑的滚动动画
- 移动端适配

## 配置个人信息

### 添加头像图片

有两种方式添加头像：

**方式1：使用本地图片**
1. 将头像图片放到 `public/images/` 目录下（如 `avatar.jpg`）
2. 在 `src/data/personal.ts` 中设置：
   ```typescript
   avatarUrl: '/images/avatar.jpg',
   ```

**方式2：使用在线图片 URL**
在 `src/data/personal.ts` 中直接设置图片 URL：
```typescript
avatarUrl: 'https://example.com/your-avatar.jpg',
```

**不设置头像**
如果不设置 `avatarUrl` 或设置为 `undefined`，将显示默认的 👤 图标。

### 修改个人信息

编辑 `src/data/personal.ts` 文件：
- `title`: 修改主标题/姓名
- `subtitle`: 修改副标题/简介
- `avatarUrl`: 设置头像图片路径或 URL

## 注意事项

- 保持设计简洁，不要过度设计
- 性能优化：图片使用懒加载
- 确保所有链接可点击
- 头像图片建议使用正方形（如 400x400px），格式推荐 JPG、PNG 或 WebP
